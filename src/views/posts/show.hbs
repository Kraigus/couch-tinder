<h1>{{post.title}}</h1>

<span class="entry-date block font-3-4 c-lt-gray">Written on {{post.createdAt}}</span>
<p>{{post.body}}</p>
{{#if show}}
{{#if firstName}}
<ul id="editAndDeleteUl" class="no-bullets no-padding mar-t-2">
  <li class="pipe-separate left">
    <a href="/posts/{{ post._id }}/edit" class="c-white">edit</a>
  </li>

  <li class="pipe-separate left">
    <button id="deleteEntryButton" data-entryid="{{post._id}}" value="delete"
      class="no-border no-outline no-bg c-white hover-underline">delete</button>
  </li>
</ul>
{{/if}}
{{/if}}
